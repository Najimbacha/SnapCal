{
  "name": "snapcal-security-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project snapcal-security-test --only firestore,storage \"node --test *.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.28.1"
  }
}
