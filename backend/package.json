{
  "name": "snapcal-backend",
  "version": "1.0.0",
  "description": "Secure proxy backend for SnapCal food scanning",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "set-admin": "node scripts/set-admin-claim.js",
    "migrate:dry-run": "node scripts/migrate-user-settings.js --dry-run"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "firebase-admin": "^12.0.0",
    "morgan": "^1.10.0",
    "node-cron": "^3.0.3"
  }
}
